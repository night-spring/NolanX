// ─── Backend Configuration ───────────────────────────────────────────────────
// Change this single value when deploying to a remote server.
// e.g. 'https://api.mysite.com'
const API_BASE_URL = 'http://127.0.0.1:8000';

export default API_BASE_URL;
